# chezmoi dotfiles

## Data needed

Some config files need some data. These are the keys that need to be defined in the data section of `~/.config/chezmoi/chezmoi.toml`:

- `email`: the email you want associated with the machine in question
